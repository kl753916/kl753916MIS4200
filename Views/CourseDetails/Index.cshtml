@model IEnumerable<kl753916MIS4200.Models.CourseDetail>

@{
    ViewBag.Title = "Index";
}

<h2>MIS Course Details</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.courseDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.firstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.grade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.courseDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.firstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.grade)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.courseDetailID }) |
            @Html.ActionLink("Details", "Details", new { id=item.courseDetailID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.courseDetailID })
        </td>
    </tr>
}

</table>
